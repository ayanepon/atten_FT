"""Reviewer-requested follow-up experiments for the BlackboxNLP paper.

This package is intentionally separate from the canonical paper pipeline.  All
commands write to a reviewer-followup output root and never overwrite the
frozen canonical results.
"""

FACTORIAL_GROUPS = ("p0f0", "p0f1", "p1f0", "p1f1")
